# Build Resources

Place your app icons here:

- `icon.ico` — Windows (256x256 recommended)
- `icon.icns` — macOS
- `icon.png` — Linux (512x512 recommended)

electron-builder will pick these up automatically during `npm run build:win/mac/linux`.
